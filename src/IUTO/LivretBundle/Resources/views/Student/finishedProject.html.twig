{# on entend base.html.twig pour avoir le menu sur la gauche #}
{% extends 'base.html.twig' %}

{# block titre pour changer le titre de l'onglet #}
{% block title %}
    Livret'O - Finition projet
{% endblock %}

{% block content %}
    <div class="div_confirm_complete">
        <a href="{{ path('iuto_livret_pdfgenerator', {'id' : projet.id}) }}" class="btn btn-success">Aperçu du compte rendu</a>
        <a href="{{ path('iuto_livret_download', {'id' : projet.id}) }}" class="btn btn-default">Enregistrer sur l'ordinateur</a>
    </div>
{% endblock %}
