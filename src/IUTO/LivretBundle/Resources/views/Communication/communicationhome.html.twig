{% extends 'base.html.twig' %}

{% block content %}
    <div class="div_home">
        <h1 class="h1_home"> {{ statutCAS|capitalize }} </h1>
        {% for o in options %}
            <a href="#" class="buttonhome">{{ o }}</a>
        {% endfor %}
    </div>
{% endblock %}