<head>
    <meta charset="utf-8">
    <style>
        .entete_projet {
            text-align: center;
            margin-bottom: 15px;
        }

        .titre {
            font-size: 45px;
            white-space: nowrap;
            margin-bottom: 0;
        }

        .client {
            margin-top: 0;
        }

        .titre_info {
            margin-bottom: 0;
        }

        .presentation_projet {
            margin-bottom: 15px;
        }

        table {
            width: 100%;
        }

        .fifty-percent {
            width: 50%;
        }
    </style>
</head>
<body>
<table>
    <tr>
        <td class="fifty-percent">
            <p> Note : Penser à gérer le fait que l'image change en fonction du dpt de réalisation du projet </p>
            <img src=".{{ asset('assets/images/Info.jpg') }}" alt="Logo IUT" style="max-height: 125px"/>
        </td>
        <td class="fifty-percent" style="text-align: right; vertical-align: top;">
            {% if etudiants is not empty %}
                <p> Étudiants </p>
                <p>
                {% for etudiant in etudiants %}
                    {{ etudiant.prenomUser }} {{ etudiant.nomUser }}<br/>
                {% endfor %}
                </p>
            {% endif %}
        </td>
    </tr>
</table>

<div class="entete_projet">
    <h1 class="titre"> {{ nom }} </h1>
    <p class="client"> À destination de {{ client }} </p>
</div>

<div class="presentation_projet">
    <h4 class="titre_info"> Description du projet </h4>
    <p> {{ descrip }} </p>
</div>

<div class="bilan_projet">
    <h4 class="titre_info"> Bilan du projet </h4>
    <p> {{ bilan }} </p>
</div>

<page_footer>
    <table>
        <tr>
            <td class="fifty-percent" style="vertical-align: bottom;">
                {% if tuteurs is not empty %}
                    <p>
                        {% if tuteurs|length > 1 %}
                            Encadrants :
                        {% else %}
                            Encadrant :
                        {% endif %}
                        {% for tuteur in tuteurs %}
                            {% if tuteur == tuteurs|last %}
                                {{ tuteur.prenomUser }} {{ tuteur.nomUser }}
                            {% else %}
                                {{ tuteur.prenomUser }} {{ tuteur.nomUser }},
                            {% endif %}
                        {% endfor %}
                    </p>
                {% endif %}
            </td>
            <td class="fifty-percent" style="text-align: right; vertical-align: bottom;">
                <p> Note : Penser à gérer le fait que le dpt change en fonction du dpt de réalisation du projet ainsi
                    que la promotion </p>
                <p> Département informatique, Deuxième année </p>
            </td>
        </tr>
    </table>
</page_footer>
</body>
